//arrow function
//when we were declarin function we we adding function name twice so to avoid that we have arrow function

const add =(a,b)=>{
    return Number(a)+Number(b);
}

const r = add(5,19);
console.log(r);